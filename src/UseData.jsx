import { useEffect, useState } from 'react';
import { csv } from 'd3';

/*
const UseData = ({ taxRate, incomeData }) => {
  const [data, setData] = useState(null);
  
  const handleIncrement = () => {
    setValue(value + 1);
  };

  const handleTaxChange = () => {
    
  };

  return data;
};

export default Data;*/
